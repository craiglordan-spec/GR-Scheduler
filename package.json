{
  "name": "gr-scheduler",
  "version": "0.1.0",
  "description": "GR Project Control — Scheduler & Timeline. Programme scheduling for Gravel Road engineering projects; reads projects & invoices from InvoiceDesk.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.22.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
